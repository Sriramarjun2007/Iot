{
  "name": "iotbench-backend",
  "version": "1.0.0",
  "description": "Backend server for IoTBench project catalogue and applications",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npm install --prefix ../frontend && npm run build --prefix ../frontend"
  },
  "dependencies": {
    "express": "^4.18.4"
  },
  "engines": {
    "node": ">=16"
  }
}
